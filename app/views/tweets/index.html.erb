<div class="d-flex aling-items-center justify-content-center">
	<h1>Tweets</h1>  
</div>

<div class="d-flex aling-items-center justify-content-center">
  
	<% if Current.user.twitter_accounts.any? %>
		<%= link_to "Scheduled a Tweet", new_tweet_path, class: "btn btn-primary" %>
	<% end %>	 

  <% if Current.user.twitter_accounts.none? %>

    <%= link_to "Connect your  Twitter Accounts", "/auth/twitter", class: "btn btn-primary" %>

  <% end %>  
</div>


<%= render @tweets %>